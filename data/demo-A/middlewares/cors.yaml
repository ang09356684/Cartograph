id: cors
kind: other
description: Permissive CORS for browser clients — allows all origins with the standard set of headers.
code_ref: internal/router/middleware_cors.go#CORSMiddleware
notes:
  - "AllowAllOrigins=true since the short-link frontend is served from arbitrary domains."
